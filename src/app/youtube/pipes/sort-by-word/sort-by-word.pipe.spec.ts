import { SortByWordPipe } from './sort-by-word.pipe';

describe('SortByWordPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByWordPipe();
    expect(pipe).toBeTruthy();
  });
});
