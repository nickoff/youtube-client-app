import { SortByCountOfViewPipe } from './sort-by-count-of-view.pipe';

describe('SortByCountOfViewPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByCountOfViewPipe();
    expect(pipe).toBeTruthy();
  });
});
